# Font Awesome webfonts

This no-build ZIP does not require webfonts.

The demo uses a small local fallback CSS file at `vendor/fontawesome/css/all.min.css`. For final deployment, either keep the fallback CSS, switch icons to inline SVGs, or manually add approved self-hosted webfont files here.
