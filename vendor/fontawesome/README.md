# Font Awesome vendor folder

This ZIP includes a minimal self-hosted fallback CSS so the PWA does not depend on a CDN.
For production, replace `css/all.min.css` and `webfonts/` with the official Font Awesome package files approved for your license and deployment.
